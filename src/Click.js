// Request 1: Click to Change Text.

export const clickToW = (setShowWelcome, setShowGoodTime) => {
  setShowWelcome(false);
  setShowGoodTime(true);
};

export const clickToH = (setShowWelcome, setShowGoodTime) => {
  setShowWelcome(true);
  setShowGoodTime(false);
};


// Request 2: Click to Show/Close Menu.

